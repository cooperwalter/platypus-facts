{
	"name": "platypus-facts",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit",
		"lint": "bunx biome check .",
		"lint:fix": "bunx biome check --write .",
		"sync-facts": "bun run src/scripts/sync-facts.ts",
		"daily-send": "bun run src/jobs/daily-send.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/bun": "^1.3.8"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {}
}
